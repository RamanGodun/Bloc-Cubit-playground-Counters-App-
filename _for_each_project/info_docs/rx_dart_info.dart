/*
==========================================================
📌 1️⃣ Основи RxDart
==========================================================

RxDart (ReactiveX for Dart) — це бібліотека для реактивного програмування, яка розширює 
стандартний `Stream` у Dart, додаючи **нові оператори для управління потоками подій** 
та **асинхронної обробки даних**.

🔹 Використовується для **ефективного керування потоками** (`streams`).
🔹 Дозволяє використовувати **debounce, throttle, switchMap, mergeMap** тощо.
🔹 Спрощує **асинхронну взаємодію** між UI та бізнес-логікою.

==========================================================
**********************************************************
==========================================================


==========================================================
📌 2️⃣ Основні концепції RxDart
==========================================================

✅ **1. Subject** — це `StreamController`, який зберігає значення та розсилає його підписникам.
   ➜ Альтернатива для `StreamController`, що підтримує **мультикастинг** (можна підписати кілька слухачів).
   
✅ **2. Observable** — потік (`Stream`), який підтримує **реактивні оператори**.

✅ **3. Stream Transformation** — перетворення потоків даних через **оператори** 
    (`map`, `flatMap`, `switchMap`, `debounceTime`, `throttleTime`).

✅ **4. Подписки (Subscriptions)** — спосіб отримати дані зі стріма (`listen()`).

✅ **5. CompositeSubscription** — керує кількома підписками одночасно, щоб уникнути `memory leak`.

==========================================================
**********************************************************
==========================================================


==========================================================
📌 3️⃣ Основні типи Subject'ів у RxDart
==========================================================

RxDart має кілька типів `Subject`, кожен з яких виконує унікальні функції:

| **Тип**                | **Опис**                                                     | **Приклад використання**                                       |
|------------------------|--------------------------------------------------------------|----------------------------------------------------------------|
| **PublishSubject**     | Передає лише **нові події** (без кешу)                       | `PublishSubject<int> subject = PublishSubject();`              |
| **BehaviorSubject**    | Зберігає **останнє значення**, яке отримують нові підписники | `BehaviorSubject<int> subject = BehaviorSubject.seeded(0);`    |
| **ReplaySubject**      | Зберігає **історію** значень (можна встановити буфер)        | `ReplaySubject<int> subject = ReplaySubject<int>(maxSize: 2);` |

🔹 **Якщо потрібно кешувати значення для нових підписників — використовуйте `BehaviorSubject`**.
🔹 **Якщо потрібно передавати тільки нові події — `PublishSubject`**.
🔹 **Якщо потрібно зберігати історію подій — `ReplaySubject`**.

==========================================================
**********************************************************
==========================================================


==========================================================
📌 4️⃣ Основні оператори RxDart (та їх аналоги у bloc_concurrency)
==========================================================

| **Оператор RxDart**    | **Опис**                                             | **Аналог у bloc_concurrency** |
|------------------------|------------------------------------------------------|-------------------------------|
| **debounceTime**       | Затримує події, залишаючи тільки останню після паузи | `restartable()`               |
| **throttleTime**       | Пропускає тільки першу подію у вказаний період       | `droppable()`                 |
| **map**                | Перетворює дані | `mapEventToState`                  |                  -------------            
| **switchMap**          | Відміняє попередній потік, якщо приходить нова подія | `restartable()`               |
| **mergeMap**           | Обробляє всі події одночасно                         | `concurrent()`                |
| **concatMap**          | Виконує події по черзі                               | `sequential()`                |

==========================================================
**********************************************************
==========================================================


==========================================================
📌 5️⃣ Коли використовувати RxDart?
==========================================================

✅ **RxDart добре підходить для:**
- Автоматичного **об’єднання потоків даних** (`mergeMap`, `switchMap`).
- **Throttle & debounce** для введення тексту або натискань кнопок.
- Реалізації **реактивного стану** у Flutter (`BehaviorSubject`).
- Кешування останнього стану **без окремої змінної** (`BehaviorSubject`).

❌ **Коли НЕ потрібно використовувати RxDart?**
- Якщо у вас простий додаток, де достатньо `Future` або `Stream`.
- Якщо `BLoC` і `bloc_concurrency` дають усі необхідні інструменти для керування потоками.

==========================================================
**********************************************************
==========================================================


📌 **Висновок**  
RxDart **корисний для складних сценаріїв** реактивного програмування.  
🔹 Використовуй **debounce** для пошукових запитів.  
🔹 Використовуй **throttle** для обмеження частих натискань.  
🔹 Використовуй **BehaviorSubject** для збереження стану.

🚀 **Якщо тобі потрібен максимальний контроль над потоками подій — використовуй RxDart!**





==========================================================
📌 **RxDart** через аналогію труби з водою
==========================================================

### 🏗️ Основна концепція RxDart: "Труби, вода і клапани"

Якщо представити **асинхронні події** у вигляді **води, що тече по трубах**, то **RxDart** — це набір інструментів, які дозволяють **контролювати, фільтрувати, комбінувати і змінювати цей потік**.

🔹 **Stream** (потік) – це сама труба, по якій тече вода (дані).  
🔹 **Observable** – це труба, що передає дані в реактивному стилі.  
🔹 **StreamController / Subject** – це головний вентиль (керує подачею води).  
🔹 **Оператори трансформації** – це різні клапани, які регулюють, куди і як тече вода.  

---
## 🛠️ Основні компоненти **RxDart** через аналогію водопроводу:
### 1️⃣ **Stream (Труба, по якій тече вода)**
   - Це **джерело даних**, що передає події в потік.
   - Аналогічно, як труба передає воду.

### 2️⃣ **StreamController / Subject (Головний вентиль)**
   - **StreamController** відкриває або закриває подачу даних.
   - **Subjects** — це розумні вентилі:
     - `BehaviorSubject` – як бак із водою, що завжди містить **останню краплю**.
     - `PublishSubject` – як труба **без резервуару**, пропускає **тільки нові події**.
     - `ReplaySubject` – як бак, що зберігає **історію подій** (наприклад, останні 10 літрів води).
     
### 3️⃣ **Оператори трансформації (Клапани та розгалужувачі)**
   - **debounceTime()** – як клапан із затримкою: не пускає нову воду, поки не пройде заданий час.
   - **throttleTime()** – як обмежувач потоку: пропускає воду з певною періодичністю.
   - **mergeMap()** – як дві труби, які об’єднують потоки даних.
   - **switchMap()** – якщо приходить новий запит, стара вода скидається, а труба підключається до нової.
   - **concatMap()** – новий потік не починається, доки не завершиться попередній.

---
## 🏗️ **Як RxDart дозволяє вставляти одну трубу в іншу?**
- **CompositeSubscription** – це механізм, що дозволяє **поєднувати кілька потоків даних**.
- Це **як з'єднання кількох труб**, що можуть передавати дані паралельно або послідовно.

```dart
final subject = PublishSubject<int>();
final anotherSubject = BehaviorSubject<int>();

 Об'єднуємо два потоки
final mergedStream = Rx.merge([subject, anotherSubject]);

mergedStream.listen((event) {
  print('Отримано значення: $event');
});

Додаємо дані у кожен стрім
subject.add(1);
anotherSubject.add(2);
 
*/
